@model MyJournal.WebApi.Models.Subject.AssignTeacherModel

@{
    ViewData["Title"] = "Додати викладача";
}

<h1>Додати викладача до предмету</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Assign">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label class="control-label">Викладач</label>
                @Html.PartialAsync("TeacherSelectionPartial").Result
            </div>
            <div class="form-group">
                <label class="control-label">Предмет</label>
                @Html.PartialAsync("SubjectSelectionPartial").Result
            </div>
            <div class="form-group">
                <input type="submit" value="Додати" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Назад</a>
</div>

